import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    listView: {
        paddingTop: 30,
        margin: 5,
        flexDirection: 'row',
        flexWrap: 'wrap'
    },
    loading: {
        flex: 1, 
        paddingTop: 20
    }
});

export default styles;